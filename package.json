{
  "name": "svg-to-swiftui-cli",
  "version": "1.0.0",
  "description": "SVG to SwiftUI Converter CLI",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/"
  ],
  "bin": {
    "svg-to-swiftui-cli": "build/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": {
    "name": "Antoni Silvestrovic",
    "email": "antoni.silvestrovic@gmail.com",
    "url": "http://antoni.ai"
  },
  "license": "MIT",
  "dependencies": {
    "meow": "^9.0.0",
    "svg-to-swiftui-core": "^0.2.4"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  }
}
